import {useEffect,useMemo,useRef,useState} from "react";
import {Apple,ArrowLeft,Check,ChevronRight,Clock3,Edit3,MinusCircle,X} from "lucide-react";
import {api,nutritionApi,type Entry,type MealInput,type MealType,type TodayMeal} from "./api";

const moments:Array<{id:MealType;label:string;matches:string[]}>= [
 {id:"BREAKFAST",label:"Desayuno",matches:["BREAKFAST","DESAYUNO"]},
 {id:"MID_MORNING",label:"Media mañana",matches:["MID_MORNING","MEDIA MAÑANA"]},
 {id:"LUNCH",label:"Comida",matches:["LUNCH","COMIDA"]},
 {id:"SNACK",label:"Merienda / snack",matches:["SNACK","MERIENDA"]},
 {id:"DINNER",label:"Cena",matches:["DINNER","CENA"]},
 {id:"OTHER",label:"Otro",matches:["OTHER","OTRO"]},
];

export function MealFlow({meals,editing,onClose,onDone}:{meals:TodayMeal[];editing?:Entry|null;onClose:()=>void;onDone:()=>void}){
 const [moment,setMoment]=useState<MealType|undefined>(editing?"OTHER":undefined),[mode,setMode]=useState<"proposal"|"substitute"|"custom">(editing?"custom":"proposal"),[busy,setBusy]=useState(false),[error,setError]=useState(""),[skip,setSkip]=useState(false);const close=useRef<HTMLButtonElement>(null);
 useEffect(()=>{close.current?.focus();const key=(e:KeyboardEvent)=>{if(e.key==="Escape"&&!busy)onClose()};document.body.classList.add("modal-open");addEventListener("keydown",key);return()=>{document.body.classList.remove("modal-open");removeEventListener("keydown",key)}},[busy,onClose]);
 const planned=useMemo(()=>meals.find(meal=>meal.status==="PENDING"&&moments.find(x=>x.id===moment)?.matches.includes(meal.meal_type.toUpperCase())),[meals,moment]);
 const run=async(action:()=>Promise<unknown>)=>{if(busy)return;setBusy(true);setError("");try{await action();onDone()}catch(cause){setError(cause instanceof Error?cause.message:"No se pudo guardar la comida")}finally{setBusy(false)}};
 const heading=!moment?"¿Qué comida es?":planned&&mode==="proposal"?"Tu comida prevista":editing?"Editar comida":"Registrar comida";
 return <div className="overlay meal-flow-overlay" role="presentation"><section className="modal meal-flow" role="dialog" aria-modal="true" aria-labelledby="meal-flow-title"><div className="modal-head"><div><small>NUTRICIÓN DE HOY</small><h2 id="meal-flow-title">{heading}</h2></div><button ref={close} type="button" onClick={onClose} aria-label="Cerrar"><X/></button></div>
 {!moment?<MomentPicker choose={setMoment}/>:<>
  <button className="meal-back" type="button" onClick={()=>{setMoment(undefined);setMode("proposal");setSkip(false)}}><ArrowLeft/>Cambiar momento</button>
  {planned&&mode==="proposal"?<Proposal meal={planned} busy={busy} skip={skip} setSkip={setSkip} complete={()=>void run(()=>nutritionApi.completeToday(planned.planned_meal_id))} skipMeal={(notes)=>void run(()=>nutritionApi.skipToday(planned.planned_meal_id,notes))} substitute={()=>setMode("substitute")}/>:<MealForm moment={moment} planned={mode==="substitute"?planned:undefined} editing={editing} busy={busy} error={error} save={(input)=>void run(()=>planned?nutritionApi.substituteToday(planned.planned_meal_id,input):editing?api.update(editing.id,{type:"MEAL",title:input.name,entryDate:input.date||new Date().toISOString().slice(0,10),value:input.calories,unit:"kcal",details:input.portion,notes:input.notes,completed:true}):nutritionApi.customMeal({...input,mealType:moment}))}/>}
  {error&&mode==="proposal"&&<div className="error" role="alert">{error}</div>}
 </>}</section></div>
}

function MomentPicker({choose}:{choose:(value:MealType)=>void}){return <div className="moment-grid">{moments.map(moment=><button type="button" onClick={()=>choose(moment.id)} key={moment.id}><Clock3/><span>{moment.label}</span><ChevronRight/></button>)}</div>}

function Proposal({meal,busy,skip,setSkip,complete,skipMeal,substitute}:{meal:TodayMeal;busy:boolean;skip:boolean;setSkip:(value:boolean)=>void;complete:()=>void;skipMeal:(notes:string)=>void;substitute:()=>void}){const [notes,setNotes]=useState("");return <div className="planned-proposal"><div className="proposal-card"><Apple/><div><small>{meal.meal_name}</small><h3>{meal.recipe}</h3><p>Ración × {meal.portion_multiplier||1}</p></div><strong>{Number(meal.calories||0).toFixed(0)}<small>kcal</small></strong></div><div className="macro-strip"><span><b>{Number(meal.protein||0).toFixed(0)} g</b><small>Proteína</small></span><span><b>{Number(meal.carbohydrates||0).toFixed(0)} g</b><small>Carbohidratos</small></span><span><b>{Number(meal.fat||0).toFixed(0)} g</b><small>Grasas</small></span></div>{skip?<div className="skip-panel"><label>Nota opcional<textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder="¿Por qué te la has saltado?"/></label><div><button type="button" onClick={()=>setSkip(false)}>Cancelar</button><button type="button" disabled={busy} onClick={()=>skipMeal(notes)}>Confirmar saltada</button></div></div>:<div className="proposal-actions"><button className="meal-yes" type="button" disabled={busy} onClick={complete}><Check/>{busy?"Guardando…":"Sí, he comido esto"}</button><button type="button" disabled={busy} onClick={substitute}><Edit3/>He comido otra cosa</button><button type="button" disabled={busy} onClick={()=>setSkip(true)}><MinusCircle/>Me la he saltado</button></div>}</div>}

function MealForm({moment,planned,editing,busy,error,save}:{moment:MealType;planned?:TodayMeal;editing?:Entry|null;busy:boolean;error:string;save:(input:Omit<MealInput,"mealType">)=>void}){return <form className="quick-meal-form" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget),number=(name:string)=>f.get(name)?Number(f.get(name)):undefined;save({name:String(f.get("name")),date:String(f.get("date")),portion:String(f.get("portion")||""),calories:number("calories"),protein:number("protein"),carbohydrates:number("carbohydrates"),fat:number("fat"),notes:String(f.get("notes")||"")})}}><p>{planned?<>Sustituye <b>{planned.recipe}</b>. Conservaremos la referencia al plan original.</>:<>Registra tu {moments.find(x=>x.id===moment)?.label.toLowerCase()} sin campos innecesarios.</>}</p><label>¿Qué has comido?<input required autoFocus name="name" defaultValue={editing?.title||""} placeholder="Ej. Yogur con fruta"/></label><div className="meal-form-row"><label>Fecha<input required type="date" name="date" defaultValue={editing?.entryDate||new Date().toISOString().slice(0,10)}/></label><label>Cantidad o ración<input name="portion" defaultValue={editing?.details||""} placeholder="Ej. 1 bol"/></label></div><div className="macro-fields"><label>Kcal<input type="number" min="0" inputMode="decimal" name="calories" defaultValue={editing?.value}/></label><label>Proteína (g)<input type="number" min="0" step="0.1" inputMode="decimal" name="protein"/></label><label>Carbos (g)<input type="number" min="0" step="0.1" inputMode="decimal" name="carbohydrates"/></label><label>Grasas (g)<input type="number" min="0" step="0.1" inputMode="decimal" name="fat"/></label></div><label>Notas opcionales<textarea name="notes" defaultValue={editing?.notes} placeholder="Cambios, sensaciones…"/></label>{error&&<div className="error" role="alert">{error}</div>}<button className="primary meal-save" disabled={busy}>{busy?"Guardando…":planned?"Guardar sustitución":"Guardar comida"}</button></form>}
